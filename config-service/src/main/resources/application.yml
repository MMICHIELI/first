spring:
  profiles:
    active: native

  cloud:
    config:
      server:
        git:
          uri: https://github.com/MMICHIELI/first
        native:
          search-locations: classpath:/config/{profile}
