spring:
  banner:
    location: classpath:/config/mmibanner.txt
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/{profile}