spring:
  profiles:
    active: dev, native

  cloud:
    config:
      server:
        git:
          uri: https://github.com/MMICHIELI/first.git
        native:
          search-locations: classpath:/config/{profile}²&