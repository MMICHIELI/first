spring:
  cloud:
    config:
      name: zuul-gateway
      uri: http://localhost:7777

      discovery:
        service-id: config-service
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka