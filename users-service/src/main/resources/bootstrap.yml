server:
  port: 3001

spring:
  application:
    name: users-service
  cloud:
    config:
      uri: http://localhost:7777

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka